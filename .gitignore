*bak
*.wrl

# copperhead: machine-specific model/endpoint config
.env

# copperhead: per-run logs, regenerated on every invocation
.copperhead/runs/

.history/

# KiCad local state (per-machine view settings) and autosave backups
*.kicad_prl
*-backups/
fp-info-cache
_tmp_verify.*
*.kicad_pcb.kicad_pro
*.kicad_pcb.bak

# scripted board saves write here before renaming onto the real board
*.kicad_pcb.tmp

# kicad-cli check output, regenerated on demand
drc_report*.json
*-drc.rpt
*-erc.rpt
